<%= form_for(@crawler_session) do |f| %>
  <% if @crawler_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawler_session.errors.count, "error") %> prohibited this crawler_session from being saved:</h2>

      <ul>
      <% @crawler_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
